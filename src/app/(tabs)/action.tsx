// File pancingan (dummy) agar Expo Router tidak memunculkan warning.
// Halaman ini tidak akan pernah dirender karena tombol "+" langsung membuka Modal.

export default function ActionScreen() {
  return null;
}
